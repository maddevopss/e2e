{
  "name": "madsuite-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:auth": "playwright test tests/auth.setup.js",
    "test:responsive": "playwright test tests/responsive-mobile.spec.js",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "dotenv": "^17.2.0"
  }
}
